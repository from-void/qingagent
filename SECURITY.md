# 安全政策（Security Policy）

## 信任模型

> **⚠️ 部署安全警告：QingAgent 当前仅按单用户、单租户设计，不提供用户间的数据或权限隔离。`QINGAGENT_AUTH_TOKEN` 只是全有全无的共享密钥，不代表独立用户身份，也不限制会话归属；任何持有密钥、能访问后端 API 的人都可以读取、修改和删除全部会话及文档，并可能消耗部署者配置的模型 key 用量。切勿以多租户形态部署到公网。多用户场景需等待规划中的 principal 身份与授权模型。**

设计前提是“部署者 = 唯一可信用户”。公网反代仅适合这位用户从自己的可信设备访问，不会把服务转变为多人隔离系统。

## 默认安全边界

默认后端只监听回环地址，仅本机可访问。公网部署必须显式加固，包括 HTTPS 反代、强随机 `QINGAGENT_AUTH_TOKEN` 和可信 Origin 配置。部署说明见 README 的[安全声明](README.md#安全声明security)。

## 桌面端自动更新信任模型

桌面端自动更新当前**未使用代码签名**。Windows 与 Linux 客户端通过 `electron-updater` 从 GitHub 仓库 `void2anything/qingagent` 的 Release 获取更新；更新元数据中的 `sha512` 用于校验下载文件完整性。macOS 未签名构建不自动安装更新，仅提示用户手动下载。

因此，自动更新信任 GitHub 的 TLS 连接、GitHub Release 内容以及该仓库的发布权限。攻击者若能控制仓库发布权限、GitHub 账户或 TLS 终端，可能发布能通过上述完整性校验的恶意更新；`sha512` 不等同于独立签名，也不能降低这一风险。

强制更新策略只从固定的 GitHub Raw HTTPS 地址 `https://raw.githubusercontent.com/void2anything/qingagent/main/update-policy.json` 读取。客户端拒绝其它域名、仓库、路径、查询参数和重定向；策略不可用时 fail-open，不会阻止常规更新检查。该策略本身没有签名，仍与 GitHub 仓库发布权限处于同一信任边界。

在引入受保护的签名密钥和平台代码签名基础设施前，发布者应保护 GitHub 账户、启用多因素认证并限制发布权限；高风险环境应从受信任渠道独立核验发布文件的校验值。

## 漏洞报告

请将安全问题发送到 `security@qingagent.com`。请不要在公开 issue、讨论区或社交媒体披露未修复漏洞。

建议报告中包含：

- 受影响版本或 commit。
- 复现步骤、影响范围和必要日志。
- 是否已经公开披露或被第三方知晓。

我们会尽量在 3 个工作日内确认收到报告，并在完成初步评估后同步修复计划或需要补充的信息。

## 支持范围

安全修复仅面向最新 `main` 分支。历史版本和私有 fork 不承诺单独维护。
