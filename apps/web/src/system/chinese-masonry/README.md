# chinese-masonry — web 内部基础组件

本组件最初从本机独立 `chinese-masonry` 仓库收编，原源码采用 MIT 许可。
现作为 `apps/web/src/system/chinese-masonry/` 下的 web 内部基础组件维护，不再独立发布。
当前保留首页画廊使用的卡片渲染、模板注册与选择代码。
精选背景图由项目作者使用 AI 生成。
运行时资产统一位于 `apps/web/public/chinese-masonry-assets/`。
