
export type LegacySection = { "kind": "h1", "data": { text: string, } } | { "kind": "h2", "data": { text: string, anchor: string | null, } } | { "kind": "p", "data": { text: string, } } | { "kind": "table", "data": { head: Array<string>, rows: Array<Array<string>>, } } | { "kind": "code", "data": { body: string, language?: string | null, } } | { "kind": "penNote", "data": { text: string, } } | { "kind": "image", "data": { src: string, alt: string, caption: string | null, width: number | null, height: number | null, align?: "left" | "center" | "right" | null, } } | { "kind": "diagram", "data": { lang: string, source: string, svg: string | null, } } | { "kind": "quote", "data": { text: string, } } | { "kind": "hr", "data": Record<string, never> } | { "kind": "list", "data": { ordered: boolean, items: Array<string>, } };
